# MongoDB Atlas Connection URI
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.cosetsy.mongodb.net/ganesh_utsav?retryWrites=true&w=majority

# Server Port (assigned automatically by cloud hosts like Render/Railway/Heroku)
PORT=5000

# Node Environment
NODE_ENV=production
